<project xmlns:ivy="antlib:org.apache.ivy.ant" xmlns:if="ant:if" name="spi_flash" default="install">
	<target name="install" description="install all components">
		<copy todir="${install.documentation}" includeEmptyDirs="true">
			<fileset dir="doc" />
		</copy>
		
		<!-- Copy the template to the destination and replace the tokens. -->
		<copy file="templates/test.lua" tofile="${install.base}/${mbs.install.org.muhkuh.tests.spi_flash.module_name}.lua">
			<filterset>
				<filter token="MODULE_NAME" value="${mbs.install.org.muhkuh.tests.spi_flash.module_name}" />
				<filter token="TEST_NAME"   value="${mbs.install.org.muhkuh.tests.spi_flash.test_name}" />
			</filterset>
		</copy>
	</target>
</project>
